description = "Control cli2voice speech for the current Gemini workspace"
prompt = """
Translate the user's request into exactly one of these modes: on, off, default, plan-on, or plan-off.

Then run exactly one shell command:
node \"__CLI2VOICE_APP_CLI__\" session <mode> --provider gemini --workspace \"${GEMINI_CWD:-$PWD}\"

After the command finishes, confirm the resulting mode.
"""
